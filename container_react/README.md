# CRA based container app

This app was created with `create-react-app`. It serves as the host for
different micro frontend apps built with Module Federation Webpack feature.

- home app\
	Developed with CRA

- posts app\
	Developed with Next.js